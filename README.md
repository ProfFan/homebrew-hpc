# ProfFan Hpc

## How do I install these formulae?

`brew install proffan/hpc/<formula>`

Or `brew tap proffan/hpc` and then `brew install <formula>`.

## Documentation

`brew help`, `man brew` or check [Homebrew's documentation](https://docs.brew.sh).
